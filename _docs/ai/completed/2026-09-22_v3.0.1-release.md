# Spec: v3.0.1 release preparation (version bump only)

**Status:** approved (owner: "please continue" after PR #64 merged, 2026-09-22). **Branch:** `chore/v3.0.1-release` from `main` @ `7bdf018`.
**Skill:** `release-management` Rules 1, 3, 4. **Tag:** human-only (handover preamble); this PR stops at the bump.

## Pre-flight (Rule 3) - done 2026-09-22
- `v3.0.0` tag on remote (`b5f30bc`); GitHub Release published, 2 assets; NuGet `SignalSentinel.Scanner` 3.0.0 live.

## Scope
Move every hand-maintained version literal from 3.0.0 to 3.0.1, following the v3.0.0 precedent commit `e35f6e8`:

| Location | Change |
| --- | --- |
| `Directory.Build.props` | `<Version>3.0.1</Version>` (the SSOT) |
| `src/SignalSentinel.Scanner/SignalSentinel.Scanner.csproj` `PackageReleaseNotes` | prepend a short `v3.0.1 - Skill false positives, YAML block scalars, regex engine guard` block (5-8 lines distilled from CHANGELOG `[3.0.1]`), keep the v3.0.0 block below it |
| `src/SignalSentinel.Core/SignalSentinel.Core.csproj` | whatever `e35f6e8` changed there, moved to 3.0.1 |
| `deploy/docker/Dockerfile.scanner` | `ARG VERSION=3.0.1` (+ any label the precedent touched) |
| `README.md` | badge, `docker pull`/`docker run` tags |
| `INSTALLATION_AND_USAGE.md` | `**Version:**`, every `:3.0.0` image tag, the version table row, the `"scannerVersion"` sample |
| `SECURITY.md` | supported-versions table as the precedent did |
| `src/SignalSentinel.Scanner/DefaultRules.json` | `"version": "3.0.1"` (`rubricVersion` stays 2.0.0) |
| `hooks/.pre-commit-hooks.yaml` | `rev: v3.0.1` |
| `CHANGELOG.md` | `## [3.0.1] - 2026-09-22`; add `[3.0.1]: .../compare/v3.0.0...v3.0.1` link reference above `[3.0.0]` |

Leave alone: `docs/MIGRATION_V3.md` (3.0.0 is the migration baseline), `docs/confidence-rubric.md` (historical mention),
`RELEASE_NOTES_v*.md` (none is written for 3.0.x; the CHANGELOG section is the release body), tests, `_docs/`.

## Acceptance
1. Drift grep (Rule 1) is empty: `git grep -n -E "\b3\.0\.0\b" -- ':(exclude)CHANGELOG.md' ':(exclude)_docs' ':(exclude)docs' ':(exclude)RELEASE_NOTES*' ':(exclude)tests'` returns nothing.
2. `dotnet build -c Release` 0 warnings; `dotnet test -c Release --no-build` 1390/1390; `--version` prints `v3.0.1`.
3. No red phase: a version bump changes no behaviour, so the test-writer phase is recorded as not applicable in the delegation log.

## As implemented (2026-09-22)

- All ten locations bumped; drift grep empty; build 0 warnings; 1390/1390; `--version` prints v3.0.1.
- One correction: `SECURITY.md` historical v3.0.0 bullet restored and a v3.0.1 bullet added above it; NuGet notes gained a CHANGED line for the migration items.
- Tag `v3.0.1` is the owner's action after merge; verify artefacts afterwards per handover section 6 (NuGet index, `gh release view`, `docker manifest inspect`).
