# Bootstrap Conflicts

This file was generated by `bootstrap.sh` during a merge of the
`generic` template into an existing project.

**Generated:** 2026-07-28T15:53:55Z
**Template:** generic
**Target:**   C:\Sites\Sentinel-Scanner

The following files exist in BOTH the existing project AND the template,
with different content. The bootstrap script did NOT overwrite them.
The orchestrator should review each conflict and ask the user how to
resolve it (keep existing, replace with template, or merge).

## Conflicts

| File | Template Version Available At |
|------|-------------------------------|
| `README.md` | `C:\Sites\_AIAgent-v1.10.2\templates\generic\README.md` |

## Resolution Instructions (For The Orchestrator)

For each conflict above:

1. Read both versions: the existing file in this project, and the template version at the path shown.
2. Diff them and identify what's different.
3. Ask the user one of:
   - "Keep existing" - leave the project's version untouched
   - "Replace with template" - overwrite with the template version
   - "Merge" - propose a merged version for the user to review
4. After resolution, note the decision in this file's "Resolutions" section.
5. Once all conflicts are resolved, delete this file (or move to `_docs/ai/completed/` for audit trail).

## Resolutions

(To be filled in by the orchestrator as each conflict is resolved.)

| File | Resolution | Date | Notes |
|------|------------|------|-------|
| `README.md` | Keep existing | 2026-09-19 | Project README is product documentation with badges, install steps, and rule list; the generic template adds nothing the project needs. |
