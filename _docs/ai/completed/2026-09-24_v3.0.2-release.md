# Spec: v3.0.2 release preparation (version bump only)

**Status:** approved (owner: "proceed", 2026-09-24). **Branch:** `chore/v3.0.2-release` from `main` @ `5f8a6b7`.
**Skill:** `release-management` Rules 1, 3, 4. **Tag:** pushed by the orchestrator after merge under the owner's "proceed with all" authority (2026-09-22) reaffirmed by "proceed" (2026-09-24); artefacts verified afterwards.

## Pre-flight (Rule 3) - done 2026-09-24
- `v3.0.1` tag on remote; GitHub Release published, 2 assets; NuGet `SignalSentinel.Scanner` and `Core` 3.0.1 live.

## Scope
Move every hand-maintained version literal from 3.0.1 to 3.0.2, following the 3.0.1 precedent commit `8432246` (#65):

| Location | Change |
| --- | --- |
| `Directory.Build.props` | `<Version>3.0.2</Version>` (the SSOT) |
| `src/SignalSentinel.Scanner/SignalSentinel.Scanner.csproj` `PackageReleaseNotes` | prepend a `v3.0.2 - Skill-scan noise: SS-012 shapes, SS-024 Info, grade honours score band` block (5-8 bullets distilled from CHANGELOG `[3.0.2]`, including a CHANGED line for the migration items and the strict-preset note), keep the v3.0.1 and v3.0.0 blocks below it |
| `deploy/docker/Dockerfile.scanner` | `ARG VERSION=3.0.2` |
| `README.md` | badge, `docker pull`/`docker run` tags |
| `INSTALLATION_AND_USAGE.md` | `**Version:**`, every `:3.0.1` image tag, the version table row, the `"scannerVersion"` sample |
| `SECURITY.md` | add a `**v3.0.2** skill-scan noise pass:` bullet ABOVE the v3.0.1 one (2-3 sub-bullets); leave the v3.0.1 and v3.0.0 bullets as they are |
| `src/SignalSentinel.Scanner/DefaultRules.json` | `"version": "3.0.2"` (`rubricVersion` stays 2.0.0) |
| `hooks/.pre-commit-hooks.yaml` | `rev: v3.0.2` |
| `CHANGELOG.md` | `## [3.0.2] - Unreleased` -> `## [3.0.2] - 2026-09-24`; add `[3.0.2]: https://github.com/SignalCoding/signal-sentinel-scanner/compare/v3.0.1...v3.0.2` directly above the `[3.0.1]:` link reference |

Leave alone: `docs/MIGRATION_V3.md`, `docs/confidence-rubric.md`, `RELEASE_NOTES_v*.md`, tests, `_docs/`, `.github/`, source code.

## Acceptance
1. Drift grep (Rule 1) is empty: `git grep -n -E "\b3\.0\.1\b" -- ':(exclude)CHANGELOG.md' ':(exclude)_docs' ':(exclude)docs' ':(exclude)RELEASE_NOTES*' ':(exclude)tests'` returns only the historical `v3.0.1 - ...` release-notes heading and the `v3.0.1` SECURITY.md bullet.
2. `dotnet build -c Release` 0 warnings; `dotnet test -c Release --no-build` 1424/1424; `--version` prints `v3.0.2`.
3. No red phase: a version bump changes no behaviour.

## As implemented (2026-09-24)

- Nine files bumped (Core csproj needs nothing since 3.0.0); drift grep empty; build 0 warnings; 1424/1424; `--version` prints v3.0.2. No corrections needed.
