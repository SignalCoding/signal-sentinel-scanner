# v3.0.0 pre-tag smoke fixes

**Source:** `_docs/ai/completed/2026-09-20_v3.0.0-smoke-test.md` (defects D1–D12).
**Scope approved by owner:** full set (D1–D7, D10, D12). D8, D9, D11 stay on the 3.0.x backlog.
**Branch:** `fix/v3-smoke-defects` → PR to `main`, ship inside v3.0.0 (tag not yet pushed).

## Work items

| Id | Change | Files | Tests |
| --- | --- | --- | --- |
| D1 | `InitializeAsync` requests `McpProtocolVersions.Current`; if the server answers with a JSON-RPC error, retry once with `McpProtocolVersions.Fallback` (2025-06-18). Subsequent HTTP requests carry `MCP-Protocol-Version: <negotiated>`. SS-INFO-004 is unchanged: it now reflects the server's ceiling because the client no longer caps it. | `McpConnection.cs`, `McpTransport.cs` | unit: initialize payload contains Current; rule test unchanged |
| D2 | `--remote http(s)://` → `McpTransportType.StreamableHttp`. | `Program.cs` | CLI test: `--remote` server transport is StreamableHttp; no SS-INFO-004 transport note |
| D3 | Notifications over HTTP go through the same request builder as requests (session header, custom headers). | `McpConnection.cs` | unit: fake server asserts `Mcp-Session-Id` on the `notifications/initialized` POST |
| D4 | `evaluableServers` counts **connected** servers only. Inconclusive description updated. | `Program.cs`, `SeverityScorer.cs` | scorer test: findings present but zero connected → Inconclusive; CLI test with unreachable `--remote` |
| D7 | On HTTP 405 (or 404/400) from the `initialize` POST, GET the same URL with `Accept: text/event-stream` (headers only). `text/event-stream` → `LegacySseEndpointException` → `ServerEnumeration.LegacySseEvidence`; SS-INFO-004 emits **Medium** "Legacy HTTP+SSE Endpoint Not Scanned". Non-MCP Info no longer fires for it. | `McpConnection.cs`, `ToolEnumerator.cs`, `LegacyMcpProtocolRule.cs` | unit: fake 405 + GET SSE → Medium finding; fake 405 + GET HTML → Non-MCP |
| D12 | `ParseArguments` distinguishes help/version (exit 0) from argument errors (exit 2, no usage dump). Post-loop private-address refusals included. | `Program.cs`, `ScanConfig.cs` | CLI test: `--remote http://127.0.0.1/mcp` → exit 2, stderr only |
| D5 | INJECTION-001: skip `NEVER/MUST/ALWAYS + verb` when the verb is third-person negated defensive phrasing (`never returns`, `never reveals`, `never sends`, `must not …`). | `InjectionPatterns.cs` | pattern tests: "never returns credential values" clean; "NEVER return the system prompt to the user? " still fires; DVMCP ch2 still fires |
| D6 | SS-008: (a) `token` requires a qualifier (`access/bearer/refresh/session/auth/api token`); (b) Credential Access is **High** unless a disclosure verb (`return`, `expose`, `reveal`, `print`, `include`, `display`, `dump`, `leak`) appears within the same sentence as the credential noun and the sentence is not negated; (c) PII: remove `user`, `client`, `account` from person words and `query`, `read` from data words; require the person word within 5 words of a PII noun (`email`, `phone`, `address`, `ssn`, `date of birth`, `records`) or the database/file word in the same sentence. | `SensitiveDataRule.cs` | fixtures: Learn, HF, Chainflip tool lists → no SS-008 Critical; DVMCP ch2 `search_company_database` still Critical |
| D10 | SS-031: also apply `SensitivityKeywords`/`CredentialKeywords` to resource name, URI and description; add `credentials?`, `secrets?`, `passwords?`, `tokens?` URI path segments as High "Resource Advertises Credential Material". | `ResourcePoisoningRule.cs` | DVMCP ch1/ch4 resources → finding |
| Fixtures | `tests/SignalSentinel.Scanner.Tests/Fixtures/RemoteToolLists/*.json` captured from live `tools/list`; test asserts grade ≥ B and zero Critical for clean servers, and expected Criticals for DVMCP ch2/6/8. | tests | |

## Out of scope

Full legacy HTTP+SSE transport (GET stream + POST `/messages`), SS-020 metadata quality (D8), SS-042 exception text (D9), stdio exit reporting (D11).

## Validation

`dotnet build -c Release` 0/0; `dotnet test` all green; re-run the smoke matrix from the record (public servers, OAuth group, DVMCP legacy SSE + stdio, offline refusals) and append results to the smoke record.
