# Implementation Report: v3.0.2-skill-noise

**Date:** 2026-09-24
**Status:** GREEN (0 failed, 1413 passed - see "Correction round 1" below for the final state; the 3-red interim state from the first pass is superseded)
**Spec:** _docs/ai/specs/v3.0.2-skill-noise.md

## Files Changed

- `src/SignalSentinel.Scanner/Rules/SkillRules/SkillScopeViolationRule.cs`
- `src/SignalSentinel.Scanner/Rules/SkillRules/SkillIntegrityRule.cs`
- `src/SignalSentinel.Scanner/Scoring/SeverityScorer.cs`
- `CHANGELOG.md`
- `docs/keyword-rules.md`

## What Was Modified

| Item | file:line | Change |
| --- | --- | --- |
| N1 filesystem synonyms | `SkillScopeViolationRule.cs:44-52` | `CapabilitySynonyms["filesystem access"]` extended with `file`, `files`, `document`, `documents`, `.docx/.pptx/.xlsx/.pdf/.png/.md/.json/.csv`. |
| N1 FileSystemCapability | `SkillScopeViolationRule.cs:154-159` | `\b(?:write\|delete\|remove\|overwrite\|modify\|edit\|save\|move\|list)\b(?:\s+\S+)?(?:\s+\S+)?(?:\s+\S+)?\s+(?:files\|directory\|directories\|folders?)\b\|\b(?:read_file\|write_file\|readFile\|writeFile\|fs\.\w+\|mkdir\|rmdir\|rm\s+-rf)\b` |
| N1 NetworkCapability | `SkillScopeViolationRule.cs:167-172` | `\b(?:fetch\|download\|retrieve\|pull\|call\|query\|post\|send\|upload\|get\|hit)\b(?:\s+\S+)?(?:\s+\S+)?(?:\s+\S+)?\s+(?:https?://\|(?:the\s+)?(?:api\|endpoint\|webhook\|server\|url)\b)\|(?-i:curl\s\|wget\s)\|Invoke-WebRequest\|Invoke-RestMethod\|requests\.(?:get\|post\|put)\|httpx\.\|urllib\|fetch\(\|axios\.\|http\.(?:get\|post)` |
| N1 ShellCapability | `SkillScopeViolationRule.cs:174-179` | `\b(?:run\|execute\|invoke\|launch\|spawn)\b(?:\s+\S+)?(?:\s+\S+)?(?:\s+\S+)?\s+(?:commands?\|shell\|subprocess\|terminal\|process)\b\|subprocess\.\w+\|child_process\|os\.system\|Process\.Start\|popen\|(?:sh\|bash\|cmd\|powershell)\s+-c` |
| N2 severity | `SkillIntegrityRule.cs:61-65` | Unsigned-skill finding `Severity.Info` (was `Medium`). Title/description/remediation unchanged. |
| N5 grade ceiling | `SeverityScorer.cs:117-123` | C-by-High branch: `return score >= thresholds.C ? SecurityGrade.C : SecurityGrade.D;` (was unconditional `C`). |
| N5 description | `SeverityScorer.cs:147` | `GetGradeDescription(D)` now "...Critical findings detected, or the score fell below the C threshold. Immediate remediation required." |
| CHANGELOG | `CHANGELOG.md` | New `## [3.0.2] - Unreleased` (N1/N2/N5 Changed bullets + migration notes) and `[3.0.2]` compare link. |
| keyword-rules doc | `docs/keyword-rules.md` | New "Pruned / reshaped (v3.0.2)" section, SS-012 row. |

## Deviations From Literal Spec Text (all documented in-code at `SkillScopeViolationRule.cs:122-153`)

Forced by `RealWorldSkillCorpusTests.N3_Corpus_ProducesNoSkillScopeViolationFindings`
(zero SS-012 across the real fixture corpus), verified against every `.md` file in
`Fixtures/RealWorldSkills` and against every currently-shipped test:

1. **Filesystem verbs narrowed**: dropped `read`, `create`, `copy` (kept `write|delete|remove|overwrite|modify|edit|save|move|list`). skill-creator's unmodified SKILL.md genuinely contains "create directories" (real action), "read file X" (a quoted example in prose about triggering), "write a standalone HTML file", "copy to the output directory" - structurally identical to the required genuine-fire test and unfixable by gap width alone. No shipped test requires these verbs to fire for filesystem.
2. **Filesystem target narrowed**: dropped bare singular `file` (kept plural `files`, plus `directory`/`directories`/`folder(s)`) - same skill-creator conflict; the one required genuine test uses plural "files".
3. **Shell target narrowed**: dropped `scripts?` (kept `commands?|shell|subprocess|terminal|process`) - skill-creator's "run a script" / "run the aggregation script" is genuine prose, not a noun mention, but has no purpose declaration route; no shipped test needs "script" as target.
4. **`curl`/`wget` matched case-sensitively** (`(?-i:curl\s|wget\s)`) instead of the literal spec's case-insensitive `curl\s`/`wget\s`: the real claude-api fixture and the office-helper fixture (crafted from the spec's own FP table) both contain "a shell/cURL project" verbatim; a case-insensitive `curl\s` fires on that mixed-case noun mention. Case-sensitive lowercase still fires on the genuine test ("curl -X POST ...") and the pre-existing `EveryEmittedFinding_CarriesAstCodes` test ("Issues curl and wget requests externally.").

All four were checked against the full fixture corpus (`grep -rinoP` per shape) before and after; no other skill (academy-guide, algorithmic-art, claude-api, coauthor, mcp-builder) is affected - claude-api and mcp-builder already declare network via "api" in their descriptions, and claude-api additionally declares filesystem via "target file" in its TRIGGER text.

## Tests Left Red (pre-existing, not modified, not in the N1-N5 red baseline)

Full suite: 3 failed / 1410 passed / 1413 total. All three assert on a bare verb/token with no reachable target under any gap width - exactly the false-positive class N1 exists to remove; making them pass would require reintroducing the bare-token matching the spec explicitly excludes (and would re-break N3's zero-SS-012 corpus requirement, since e.g. bare "fetch" reappears throughout academy-guide/mcp-builder).

- `SkillScopeViolationRuleTests.Evaluate_FormatterWithNetworkAccess_ReturnsFinding` - the capability-matched text is `SegmentFilter.TextFor` over `skill.RawContent` ("Format the code. Also fetch the latest rules.", **no URL** - the URL only exists in `InstructionsBody`, a field this rule does not read), so "fetch" never reaches a target at any gap width. Not a shape-tuning issue.
- `SkillScopeViolationCaseInsensitiveTests.NoMatchingSynonym_EmitsFindingStill` - body "Runs http requests to fetch remote dependencies." is exactly the spec's own listed non-firing example ("fetch as a verb without a target").
- `SkillScopeViolationYamlTests.YamlCapabilities_UnrelatedCapability_DoesNotSuppressOtherClasses` - body "Issues fetch and https requests to external APIs." - `https` lacks `://` and `APIs` is plural (word-boundary fails), so no target is reachable.

The test-writer's own red-phase report flagged these three exact tests as an open risk for "the coding agent's/orchestrator's call." Per the "test wins... if impossible, leave it red and report" instruction, all three were left red rather than silently relaxing the shape or modifying the tests.

## Files Intentionally Not Touched

- `tests/**` - no test file or fixture edited, per instructions.
- `scoring-rubric-v2.0.0.json` - N5 explicitly keeps the rubric JSON unchanged; only the scorer's use of the existing threshold changed.
- `Directory.Build.props`, MCP rules, `Program.cs` - out of scope.

## Validation

| Command | Result |
| --- | --- |
| `dotnet build signal-sentinel.sln -c Release` | 0 Warning(s), 0 Error(s) |
| `dotnet test signal-sentinel.sln -c Release --no-build` | Failed: 3, Passed: 1410, Total: 1413 |
| `dotnet test ... --filter "FullyQualifiedName~N1_\|~N2_\|~N3_\|~N5_"` | All pass |
| `dotnet test ... --filter "FullyQualifiedName~RegexEngineIntegrityTests"` | Pass (hygiene + engine-consistency guards) |

## Dependencies Added

None.

## Follow-Up Needed

- Owner decision: are the 3 red pre-existing tests acceptable as permanently-red regression pins documenting the new exclusion boundary, or should they be edited/removed by a test-writer in a follow-up pass? They cannot be turned green without reintroducing the bare-token false-positive class N1 removes.
- `Evaluate_FormatterWithNetworkAccess_ReturnsFinding`'s `RawContent`/`InstructionsBody` divergence (the rule only reads segments derived from `RawContent`) looks like a pre-existing test-fixture inconsistency unrelated to N1; flagged for awareness, not fixed (out of scope).

## Risks and Notes

- The filesystem/shell verb and target narrowing (deviation 1-3 above) means SS-012 will no longer catch "read the file", "create a file", "copy the file" or "run a script" as filesystem/shell scope violations, even when genuinely undeclared. This trades detection breadth for corpus-verified false-positive elimination, per the N3 acceptance gate. No CHANGELOG entry beyond the general SS-012 reshaping note, since these are sub-deviations of the same N1 migration, not separate rule changes.
- `curl`/`wget` case-sensitivity is a narrow, verified-safe heuristic (corpus-checked); a skill that writes `CURL` or `WGET` in all caps as a genuine invocation (unusual) would not be caught by the concrete-client path, though the verb-shape network patterns and other concrete clients remain unaffected.

## Correction round 1 (2026-09-24)

Orchestrator ruling accepted the N1 verb-list deviations as-is (spec section 6, last
bullet) and asked for two further `NetworkCapability` shapes, plus doc updates. Only
`SkillScopeViolationRule.cs` (`NetworkCapability()` only) and the two doc files were
touched; no other file changed in this round.

### What changed

- `SkillScopeViolationRule.cs:180-186` - `NetworkCapability()` gained two alternatives:
  - `\b(?:from|to)\s+https?://` (preposition immediately before a URL)
  - `\b(?:make|makes|making|run|runs|running|issue|issues|issuing|send|sends|perform|performs)\b(?:\s+\S+)?(?:\s+\S+)?\s+(?:https?|network|api|web|rest)\s+(?:requests?|calls?)\b`
    (explicit request/call statement)
- `docs/keyword-rules.md` - added the accepted-deviations bullet list (filesystem
  verbs/target, shell target, curl/wget case-sensitivity) directly under the SS-012
  row, plus a bullet pair documenting the two new network shapes (and the `at`
  drop, see below).

### One deviation found during this round, applied and documented in-code

The coordinator's requested shape (i) included `at` alongside `from`/`to`
(`\b(?:from|to|at)\s+https?://`). Implementing it verbatim regressed
`N1_Network_BareUrlAndFetchAsVerbWithoutTarget_NoFinding` ("See the documentation
at https://example.com/guide for more information...") - a bare-URL mention this
test is specifically pinned to exclude. `at` was dropped from the alternation
(kept `from`/`to`); this is a narrower shape than requested, not a wider one, so
it cannot reintroduce any SS-012 corpus finding the coordinator's check ruled
out. Documented at `SkillScopeViolationRule.cs:166-179` and in
`docs/keyword-rules.md` under the SS-012 row.

### Final `NetworkCapability()` regex (verbatim)

```
\b(?:fetch|download|retrieve|pull|call|query|post|send|upload|get|hit)\b(?:\s+\S+)?(?:\s+\S+)?(?:\s+\S+)?\s+(?:https?://|(?:the\s+)?(?:api|endpoint|webhook|server|url)\b)
|(?-i:curl\s|wget\s)|Invoke-WebRequest|Invoke-RestMethod|requests\.(?:get|post|put)|httpx\.|urllib|fetch\(|axios\.|http\.(?:get|post)
|\b(?:from|to)\s+https?://
|\b(?:make|makes|making|run|runs|running|issue|issues|issuing|send|sends|perform|performs)\b(?:\s+\S+)?(?:\s+\S+)?\s+(?:https?|network|api|web|rest)\s+(?:requests?|calls?)\b
```
(`RegexOptions.IgnoreCase | RegexOptions.Compiled`, `matchTimeoutMilliseconds: 500`, unchanged.)

### Validation

| Command | Result |
| --- | --- |
| `dotnet build signal-sentinel.sln -c Release` | 0 Warning(s), 0 Error(s) |
| `dotnet test signal-sentinel.sln -c Release --no-build` | Failed: 0, Passed: 1413, Total: 1413 |

All three previously-red pre-existing tests are now green:
`SkillScopeViolationRuleTests.Evaluate_FormatterWithNetworkAccess_ReturnsFinding`
(went green once the test-writer aligned `RawContent` with the URL-bearing
`InstructionsBody`, confirmed by inspection - `RawContent` now contains
"...fetch the latest rules from https://api.rules.com", matched by the
preposition+URL shape), `SkillScopeViolationCaseInsensitiveTests.
NoMatchingSynonym_EmitsFindingStill` and `SkillScopeViolationYamlTests.
YamlCapabilities_UnrelatedCapability_DoesNotSuppressOtherClasses` (both via the
explicit request/call statement shape).

### Follow-up

- None outstanding from this round. The status line at the top of this report
  ("3 pre-existing tests left red") is superseded - full suite is now 0 failed.
