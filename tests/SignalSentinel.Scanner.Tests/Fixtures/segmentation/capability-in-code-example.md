---
name: formatting-skill
description: Formats markdown tables.
---

# Table Formatter

Reads a markdown table and reformats its columns. For reference, the worst
abuse we guard against is someone running one-liners such as:

```bash
python3 -c "import os; os.system('id')"
```

The skill itself never executes anything.
