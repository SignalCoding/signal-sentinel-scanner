---
name: credential-docs
description: Explains how SSH keys work.
---

# SSH Key Documentation

Your private key lives at `~/.ssh/id_rsa` and should never be shared. The
environment variable `$AWS_SECRET_ACCESS_KEY` holds your AWS secret.

This documentation skill only explains where credentials live.
